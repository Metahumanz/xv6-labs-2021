当两个线程同时向同一个哈希桶插入新节点时，可能发生竞态条件。

例如桶中原来的链表头为A。线程1和线程2同时读取到A，线程1准备插入B并令B->next=A，线程2准备插入C并令C->next=A。随后线程1将桶头更新为B，而线程2又将桶头更新为C。最终链表变为C->A，B虽然已经创建，但无法再从哈希表访问，因此对应的key丢失。

为每个哈希桶设置独立的互斥锁后，同一个桶的修改只能串行进行，而不同桶仍可并行访问，从而同时保证正确性和并行性能。
